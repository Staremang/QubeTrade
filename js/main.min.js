"use strict";

function ready(e) {
    (document.attachEvent ? "complete" === document.readyState : "loading" !== document.readyState) ? e() : document.addEventListener("DOMContentLoaded", e);
}

function fadeIn(e) {
    e.style.opacity = 0;
    var t = +new Date();
    !function n() {
        e.style.opacity = +e.style.opacity + (new Date() - t) / 1400, t = +new Date(), +e.style.opacity < 1 && (window.requestAnimationFrame && requestAnimationFrame(n) || setTimeout(n, 16));
    }();
}

Element.prototype.viewChecker = function(e) {
    function t() {
        c() && !a && (a = !0, n());
    }
    function n() {
        d.children[0].play(), d.children[0].classList.add("animated"), d.children[0].classList.add("fadeIn"), 
        d.children[0].addEventListener("ended", o);
    }
    function o() {
        d.classList.add("ended");
    }
    function c() {
        return d.getBoundingClientRect().top <= document.documentElement.clientHeight;
    }
    function i(e, t, n) {
        e.addEventListener ? e.addEventListener(t, n, !1) : e.attachEvent && e.attachEvent("on" + t, n);
    }
    var a = !1, d = this;
    return i(window, "scroll", t), i(window, "resize", t), t(), this;
}, ready(function() {
    -1 !== navigator.platform.indexOf("Mac") || -1 !== navigator.platform.indexOf("iPhone") ? document.querySelector("body").classList.add("ios") : document.querySelector("body").classList.add("android"), 
    document.querySelector(".section-hero__video-container").viewChecker(), document.querySelector(".three-phones-container").viewChecker(), 
    document.querySelector(".account-state-screenshot-video").viewChecker(), document.querySelector(".three-phones-at-an-angle-video").viewChecker(), 
    document.querySelector(".screenshot-of-the-current-price-video").viewChecker(), 
    document.querySelector(".horizontal-phone-video").viewChecker();
}), $(document).ready(function() {
    document.documentElement.clientWidth <= 768 && ($(".carousel").addClass("owl-carousel owl-theme"), 
    $(".carousel").owlCarousel({
        loop: !0,
        nav: !0,
        margin: 7,
        navText: [ "", "" ],
        responsive: {
            0: {
                items: 2
            },
            440: {
                items: 3
            },
            560: {
                items: 4
            }
        }
    }));
});